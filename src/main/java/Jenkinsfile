pipeline {
    agent {
        docker {
            image 'maven:3.9.6-eclipse-temurin-11'
            args '-v /root/.m2:/root/.m2'
        }
    }

    stages {

        stage('Git Checkout') {
            steps {
                git branch: 'main',
                    credentialsId: 'Github-Credentials',
                    url: 'https://github.com/ampymarcelo/TP_CI_CD_0011.git'
            }
        }

        stage('Build Application') {
            steps {
                bat 'mvn clean install -DskipTests'
            }
        }

        stage('Unit Tests') {
            steps {
                bat 'mvn test'
            }
        }

        stage('Build Docker Image') {
            steps {
                bat 'docker build -t ampy01/tp2-cicd:1.0.0 .'
            }
        }

        stage('Push to DockerHub') {
            steps {
                withCredentials([string(
                    credentialsId: 'dockerhubpass',
                    variable: 'dockerHubPass'
                )]) {
                    bat "docker login -u ampy01 -p %dockerHubPass%"
                    bat 'docker push ampy01/tp2-cicd:1.0.0'
                }
            }
        }
    }

    post {
        success {
            echo ' Pipeline terminé avec succès !'
        }
        failure {
            echo 'Pipeline échoué !'
        }
    }
}